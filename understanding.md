
Display header

grab each algorithm name as keys for a dictionary named algoNames 
  algorithms references /test_algorithms.js and its functions.

prompt the user to choose between Built-in algos or custom:

if Built-in:
  prompt the user to choose from funcitons inside test_algorithms.js 

  if there is no selection, go back.

  define algorithm as reference to selected function in test_algorithms.js

if Custom:
  



